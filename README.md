# Vue 3 && eslint && commitlint template

Initial project with Vue3, eslint, prettier, husky, lint-staged, commitlint.
If need change module cli, remember rewrite package scripts.

## Usage

```zsh
npm install
```
